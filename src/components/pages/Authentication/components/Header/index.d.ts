import { Component, Container } from "../../../../component"

/**
 * Заголовки РЕГИСТРАЦИЯ/ВХОД/ВОССТАНОВЛЕНИЕ ПАРОЛЯ/НОВЫЙ ПАРОЛЬ 
 */
export class Header implements Component {
  constructor(text: string)
  mount(container: Container): void
  unmount(): void
}
